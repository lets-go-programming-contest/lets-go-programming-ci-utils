sanity-branch:
	@bash ${TEST_DIR_UTILS}/sanity-branch.sh

sanity-files:
	bash ${TEST_DIR_UTILS}/sanity-files.sh

sanity-student:
	bash ${TEST_DIR_UTILS}/sanity-student.sh

build:
	bash ${TEST_DIR_UTILS}/build.sh

unit-tests:
	bash ${TEST_DIR_UTILS}/unit-tests.sh

custom-tests:
	bash ${TEST_DIR_UTILS}/custom-tests.sh

